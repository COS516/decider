#!/usr/bin/env python

from setuptools import setup



setup(name='decider',
      version='0.0.1',
      description='COS516 Decider',
      packages=['decider'],
      install_requires=['satispy', 'numpy', 'tqdm']
     )